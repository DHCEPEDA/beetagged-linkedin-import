-----> Building on the Heroku-24 stack
-----> Using buildpack: heroku/nodejs
-----> Node.js app detected
       
-----> Creating runtime environment
       
       NPM_CONFIG_LOGLEVEL=error
       NODE_VERBOSE=false
       NODE_ENV=production
       NODE_MODULES_CACHE=true
       
-----> Installing binaries
       engines.node (package.json):   18.x || 20.x
       engines.npm (package.json):    >=8.0.0
       
       Resolving node version 18.x || 20.x...
       Downloading and installing node 20.19.4...
       Bootstrapping npm >=8.0.0 (replacing 10.8.2)...
       npm 11.4.2 installed
       
-----> Restoring cache
       - npm cache
       
-----> Installing dependencies
       Installing node modules
       
       added 121 packages, and audited 122 packages in 972ms
       
       14 packages are looking for funding
         run `npm fund` for details
       
       8 vulnerabilities (5 low, 3 high)
       
       To address issues that do not require attention, run:
         npm audit fix
       
       To address all issues, run:
         npm audit fix --force
       
       Run `npm audit` for details.
       
-----> Build
       Detected both "build" and "heroku-postbuild" scripts
       Running heroku-postbuild
       
       > beetagged-app@1.0.0 heroku-postbuild
       > npm run build
       
       
       > beetagged-app@1.0.0 build
       > npx webpack --mode production
       
CLI for webpack must be installed.
  webpack-cli (https://github.com/webpack/webpack-cli)
We will use "npm" to install the CLI via "npm install -D webpack-cli".
Do you want to install 'webpack-cli' (yes/no): 
-----> Build failed
       
       We're sorry this build is failing! You can troubleshoot common issues here:
       https://devcenter.heroku.com/articles/troubleshooting-node-deploys
       
       If you're stuck, please submit a ticket so we can help:
       https://help.heroku.com/
       
       Love,
       Heroku
       
 !     Push rejected, failed to compile Node.js app.
 !     Push failed